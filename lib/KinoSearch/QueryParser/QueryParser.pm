use strict;
use warnings;

package KinoSearch::QueryParser::QueryParser;
use base qw( KinoSearch::QueryParser );

our %instance_vars = %KinoSearch::QueryParser::instance_vars;

# only here for back compat

1;

